# HANDOFF — 2026-05-07

> **PRIMÁRNÍ ZDROJ STAVU** pro novou session. Předchozí: `HANDOFF-2026-05-06.md`.

**Stav:** všechno pushnuté, **0 commitů ahead** před `origin/main`. SPIZ deploynut. Migrace aplikované.

---

## 🎯 NOVÁ SESSION — PRVNÍ KROKY

### Přečíst v tomto pořadí:
1. **TENTO DOKUMENT** — primární zdroj stavu (2026-05-07)
2. `INSTRUKCE/PRO-CLAUDINE-COACHE.md` — kontext o Petrovi (CPTSD, ADHD, time blindness)
3. `INSTRUKCE/03-moduly.md` — moduly + URL + status (rozšířeno o Spíž a UPLOAD)
4. `INSTRUKCE/HANDOFF-2026-05-06.md` — předchozí state
5. `HANDBOOK.md` — referenční doc, sekce „Stav 2026-05-06" v hlavičce

### První akce:
```bash
cd "/Users/petrperina/CLOUDS/CLOUDE PROJECTS/raseliniste"
git fetch origin && git status
git log --oneline -10
```

---

## 🟢 SESSION 2026-05-07 — UDĚLÁNO (15+ commitů)

### Spíž — sdílení souborů (`55f7aad`)
- **Nový modul** `/spiz` v sidebaru (mintová archive ikona, sekce Organizace)
- Drag-drop nebo file picker upload (max 500 MB, jakýkoli typ)
- Po uploadu **auto-clipboard share URL** (`/g/<token>`)
- Public stránka `/g/<token>` — G logo + „Vítejte ve spíži Gideona." + „Stáhněte si nasdílený soubor."
- Cron `cleanup-spiz` (denně 3:10) — automatické mazání po 14 dnech
- Schema: `SharedFile` (token base64url 24 znaků, expiresAt, downloadCount)
- Migrace `add_shared_file`

### UPLOAD recordings (`b94fffd`)
- **Třetí typ záznamu** v `RecordingType` enumu (vedle STANDARD/BRIEF) pro hotové audio soubory
- Host i admin nahraje MP3/M4A/WAV/... → **JEN přepis**, žádná Stage 2 AI analýza
- Audio + přepis se uchovávají natrvalo (cleanup-audio cron filtruje jen STANDARD)
- Per-host permission flag `ProjectInvitation.canUploadAudio` (default false)
- V Prskavce automaticky pro Petra
- Lavender tint v listu, „📎 Upload" badge, název = `uploadedFilename` z disku
- Endpointy: `POST /api/studna/[id]/upload-audio` (owner) + `POST /api/me/[token]/upload-audio` (host)
- Lib `transcribeAudioOnly()` v audio-transcribe.ts + `processUploadAudio()` v process-recording.ts
- Migrace `add_upload_recording` (enum value + canUploadAudio + uploadedFilename)

### Skrytí processing kolečka napříč Studánkou (`b94fffd`)
- Petrovo CPTSD: točící se kolečko = matoucí *„můžu odejít z stránky?"*
- RecordingCard processing — kolečko schované, jen tichá karta + křížek pro zrušení
- Summary processing — text *„Souhrn se připravuje na pozadí. Vrať se za chvíli — sám se objeví."*
- Myší entries + evaluations — kolečko schované, jen krátký tichý text
- GuestRecordings: processing/error label úplně schovaný od hosta (`hideErrors=true` na `/me/<token>`)
- Polling à 5 s (4 s u Myší) zůstává — UI se sama tichoaktualizuje

### Auto-retry erroru cron (`19c2c07`)
- `retry-stuck-recordings` (à 15 min) chytá teď i `status="error"` mladší 24 h
- Cooldown 60 min mezi retry pokusy (zabrání busy-loop kdy Gemini stále padá)
- Před retry status reset na "processing" → updatedAt bumpne, UI ukáže loader
- MAX_RETRIES_PER_RUN 5 → 8

### AI dotaz nad projektem (`19c2c07`)
- Endpoint `GET /api/studna/[id]/ask?q=...` → odhad input tokens + cost ($)
- `POST /api/studna/[id]/ask` body `{ q }` → odpověď
- Kontext: všechny processed záznamy s autorem/datem/typem/summary/transkriptem
- Gemini 2.5 Pro pricing v lib (May 2026): $1.25/$2.50 per 1M input, $10 per 1M output
- Threshold 200k tokens pro vyšší rate
- System prompt: věcný, češtinou, citace `[DD.M. · autor] „doslova"`, „v záznamech není" pokud info chybí
- UI v záložce Záznamy: collapsible card *„Zeptat se projektu (AI nad N záznamy)"* — textarea + „Spočítat cenu" → estimate banner → „Spustit dotaz ($X)" → answer

### Karty záznamů collapse-default (`19c2c07`)
- RecordingCard má teď `expanded` state, default false
- V hlavičce zobrazená 2řádková preview (summary nebo začátek transkriptu)
- Klik = rozbalí strukturovaný rozbor + transcript + audio
- Tlačítko „sbalit záznam" v expanded stavu
- Status badges (chyba/zpracovává) zůstávají v hlavičce vždy viditelné

### Export přepisů Studánky/Prskavky (`02bf2e7`)
- `GET /api/studna/[id]/export-transcripts` → Markdown soubor se VŠEMI přepisy
- Hlavička s názvem projektu, počtem záznamů (X standard · Y brief), datem
- Per záznam: typ + datum + autor + délka + plný transkript
- BEZ AI analýzy — jen čistý text pro další zpracování
- UI: tlačítko **Stáhnout .md** v záložce Nastavení projektu (nad Smazat-projekt řadou)

### PDF export Myší — krásný layout (`dd73dda` + `747d5f6` + `0e79d2c`)
- Nová stránka `/bwmys/[id]/tisk?print=1` — server-rendered Astro page v Base layoutu
- Header + Verdikt na 1. stránce (verdikt nahoru, hned vidět)
- 2. stránka: Decision Compass full-page
- 3. stránka: 2×2 grid (Six Hats radar + MoodCurve + EntryTypesDonut + ArgumentsGrid)
- Sekce A-H pěkně rozložené přes další stránky (explicit `break-before: page`)
- Tabulka argumentů: thead se opakuje na každé stránce, řádky se nelámou
- `print-color-adjust: exact` zaručí barvy v PDF
- Auto-print delay 1800 ms (Recharts hydration)

### Decision Compass (`1240448`)
- 5. vizualizační komponenta v B&W Myš (custom SVG, ne Recharts)
- 4 kvadranty: SZ silný PROTI, SV silný PRO, JZ šum strach, JV šum euforie
- V centru verdikt podle `decision.status` + label „opřený o sever/východ/jih/západ"
- Mapování: X=340+smer·300, Y=300+(0.5−konz)·480, R=min(32,10+cetnost·2)
- `DecisionArgument.klobouk` (Six Hats kategorie) přidaný v `extractArguments()`
- COMPASS_HAT_COLORS hex z PDF spec

### Diagnostika `/api/diagnose/bwmys` (`108248d`)
- JSON s počtem stuck processing rows + errory za 24h + migrace check + Gemini mode
- Conclusions auto-vyhodnocení (🟢/🟡/🔴 emoji)
- UI stránka `/settings/diagnose` (sidebar)

### Návody modul (`db0a57f`)
- `/navody` — grid 15 pastelových boxíků
- `/navody/<slug>` — detail se 6 sekcemi (Co to je / Jak to ovládám / Proč / Co umí / Co neumí / Napojení)
- Obsah v `src/lib/navody.ts` (Petr edituje)

### Studánka admin text input (`8a241bd`)
- Tlačítko „Vložit text místo nahrávky" v záložce Záznamy
- Endpoint `POST /api/studna/[id]/recording-text` — Stage 2 only nad textem
- `analyzeTranscript()` lib funkce
- Lavender card s textareou + select STANDARD/BRIEF

### Návod /help/upload-audio (`e86580a`)
- Public stránka (bez auth) — host může otevřít z guest UI
- 7 kroků step-by-step pro Voice Recorder app
- Tabulka 3 doporučených cílů uložení + 3 zakázané
- Print-friendly design pro Cmd+P → Save as PDF
- INSTRUKCE/UPLOAD-AUDIO-NAVOD.md = primární MD zdroj

### Upload audio i pro Úkoly + Deník (`aaedbd9`)
- DiktatRecorder + TaskAudioRecorder měly file input zaschovaný — teď prominentní lavender card
- V boardu `/ukoly` a `/denik` druhé tlačítko **„📎 Nahrát soubor"** vedle „Nadiktovat"
- Vede na `/ozvena?mode=task&upload=1` (resp. journal) — auto-otevře file picker po 250 ms

---

## 🟧 OTEVŘENÉ TODO (co jsme NEUDĚLALI)

### 🔴 Vysoká priorita / nedořešeno

1. **Things-import errors** — Petr měl ~30% error rate u `raseliniste-import.json` z předchozí session. Nikdy jsme se k tomu nevrátili — Petr má ještě poslat opravený JSON. Endpointy a UI jsou připravené (Hard reset mirror + Wipe orphan tasks v `/settings/crons`).

2. **Production test po dnešním deploy** — 15+ commitů s migracemi se aplikovalo automaticky. Co Petr ještě neotestoval:
   - Spíž upload + share URL flow + 14denní cleanup
   - UPLOAD audio z host strany (`/me/<token>` checkbox v invitation)
   - PDF export Myší — zda se grafy renderují správně v PDF
   - AI dotaz nad projektem — token estimate accuracy

### 🟡 Střední priorita

3. **B&W Myš doplněk 2** — 7 bodů z `/Users/petrperina/Downloads/rozhodovaci-system-doplnek-2.md`. Odložené z minula, nepokračovali jsme.
4. **Validace přepisu kvalita** — Petr nahlásil zaměnění subjekt/objekt ("platil za mě" vs "platil jsem za něj"). Stage 1 prompt jsme rozšířili o kritická pravidla, ale neověřili reálnou účinnost na produkčních datech.

### 🟢 Latent / Nice-to-have

- **Recurring tasks v Todoistu** — naše DB má jen single `dueAt`, recurrence se rozjede
- **DELETE leak když Todoist down** — lokální delete proběhne, Todoist task zůstane
- **Idempotency-Key header** při createTask (Todoist v1)
- **Backfill RAG indexu** pro existující data
- **Per-projekt ikona Studny**
- **Automatický backup DB** (nightly pg_dump → druhý NAS / Backblaze B2)
- **Vlastní opakující se kalendářní eventy** (jen Anniversary + CustomRitual zatím)
- **Per-recording prompty** v Studně — Petr custom obvykle nemění
- **Migrace na diego** — Petr plánuje přesun raseliniste z SPIZ na Hetzner/Coolify (diego). Při přesunu projít `_mediaface-deploy-guide/`.

---

## 🛡 BEZPEČNOSTNÍ AUDIT (provedeno 2026-05-07)

| Endpoint | Auth | Rate limit | Kontrola |
|---|---|---|---|
| `POST /api/spiz/upload` | session.uid (owner) | žádný | 500 MB cap, blacklist nebezpečných ext (.exe, .sh, .bat, atd.) ✓ |
| `GET /api/spiz` | session.uid (owner) | — | jen vlastní soubory ✓ |
| `DELETE /api/spiz/[id]` | session.uid + ownership check | — | ✓ |
| `GET /api/spiz/d/[token]` | **public** by design | — | 410 pokud expirováno; defense-in-depth: token base64url 24 znaků = 144 bit entropie ✓ |
| `POST /api/cron/cleanup-spiz` | x-cron-key header | — | ✓ |
| `POST /api/studna/[id]/upload-audio` | session.uid + ownership | — | 500 MB cap, mime audio/* whitelist ✓ |
| `POST /api/me/[token]/upload-audio` | guestToken + invitation.canUploadAudio=true (403 jinak) | **20/h per guest** | 500 MB cap, mime audio/* whitelist ✓ |
| `POST /api/studna/[id]/ask` | session.uid + ownership | — | ✓ |
| `GET  /api/studna/[id]/ask?q=...` | session.uid (owner) | — | jen estimate, žádný side effect ✓ |
| `POST /api/studna/[id]/recording-text` | session.uid + ownership | — | min 20, max 200 000 znaků ✓ |
| `POST /api/studna/[id]/files` | session.uid + ownership | — | 200 MB cap, blacklist nebezpečných ext ✓ |
| `GET  /api/studna/files/[fileId]` | session.uid + ownership cascade přes project | — | ✓ |
| `DELETE /api/studna/files/[fileId]` | session.uid + ownership cascade | — | ✓ |
| `GET  /api/studna/[id]/export-transcripts` | session.uid + ownership | — | ✓ |
| `GET  /bwmys/[id]/tisk` | session.uid + ownership | — | ✓ |

**Migrace** (všechny aditivní, bez destructive):
- `add_invitation_keep_audio` (05-06)
- `add_decision_entry_status` (05-06)
- `add_decision_evaluation_status` (05-06)
- `add_project_file` (05-06)
- `add_health_summary_status` (05-06)
- `add_shared_file` (05-07)
- `add_upload_recording` (05-07)

**Žádné změny do auth, session, payments, nebo kalendářového sync core.** Všechny session checks přes `readSession(cookies)` (existující pattern), ownership přes `userId` v Prisma where klauzulích (existující pattern).

**Riziková místa:**
- **Spíž public download** — kdokoli s tokenem může stáhnout. **By design.** Token je dlouhý (144 bit), expirace 14 dní, žádný brute-force feasible. ✓
- **AI dotaz token cost** — Petr může nechtěně spustit drahý dotaz. Mitigován **estimate před spuštěním** (Petr vidí cenu, klikne dvakrát). ✓
- **Audio rate limit 20/h per guest** — host nemůže DDoS-it server uploady. ✓

---

## 📂 KLÍČOVÉ KOMPONENTY (rychlá orientace)

### Spíž
| Soubor | Co dělá |
|---|---|
| `src/pages/spiz/index.astro` + `src/components/SpizApp.tsx` | Admin UI (drag-drop + list) |
| `src/pages/g/[token].astro` | Public landing |
| `src/pages/api/spiz/upload.ts` | Owner upload |
| `src/pages/api/spiz/d/[token].ts` | Public download |
| `src/pages/api/cron/cleanup-spiz.ts` | 14denní mazání |
| `src/lib/cron-schedule.ts` | Job v dispatcheru |

### UPLOAD recordings
| Soubor | Co dělá |
|---|---|
| `src/lib/audio-transcribe.ts:transcribeAudioOnly()` | Stage 1 only, doslovný přepis bez cleanup |
| `src/lib/process-recording.ts:processUploadAudio()` | Fire-and-forget pipeline |
| `src/pages/api/studna/[id]/upload-audio.ts` | Owner endpoint |
| `src/pages/api/me/[token]/upload-audio.ts` | Guest endpoint (vyžaduje canUploadAudio) |
| `src/components/StudnaDetail.tsx:UploadAudioCard` | Admin UI |
| `src/components/GuestRecorder.tsx:UploadAudioGuestButton` | Guest UI |

### AI dotaz nad projektem
| Soubor | Co dělá |
|---|---|
| `src/pages/api/studna/[id]/ask.ts` | GET=estimate, POST=execute |
| `src/components/StudnaDetail.tsx:ProjectAskCard` | UI v záložce Záznamy |

### PDF export Myší
| Soubor | Co dělá |
|---|---|
| `src/pages/bwmys/[id]/tisk.astro` | Print-first stránka |
| `src/components/BwMysViz/PrintViz.tsx` | Fixed-size grafy pro print |
| `src/components/BwMysViz/DecisionCompass.tsx` | Custom SVG kompas |
| `src/lib/bwmys-colors.ts` | HAT_COLORS + COMPASS_HAT_COLORS |

---

## 💬 PRINCIPY (z PRO-CLAUDINE-COACHE.md, doplněno 05-07)

> **Petr má time blindness, CPTSD, ADHD. Designové rozhodnutí o tom je.**
>
> - Žádné notifikace nad rámec push
> - Žádné gamifikace, streaks, achievements
> - Interpretační lišty dávají FAKTA, ne soudy
> - Pixel-perfect pozicování
> - **Žádné točící se kolečko (NOVÉ 05-07)** — matoucí, „můžu odejít z stránky?". AI procesy běží na pozadí, polling tichý, UI se sama doplní.
> - Před implementací sync logiky **VŽDY ověř expected response shape z dokumentace**

### Komunikace
- **Češtinu**, stručně, věcně, žádná vata
- Jeden konkrétní krok → odpověď → další (ne 5 otázek najednou)
- Když Petr vede produkční problém, vyber jednu cestu, řekni proč, nenabízej alternativy
- **Never count commits ahead z hlavy** — vždy `git fetch origin` před tvrzením stavu

---

## 🚦 PUSH + DEPLOY workflow

```bash
# Po commitu (Claude commitnul lokálně):
# Petr: GitHub Desktop → Push origin

# Na NASu (SPIZ — server hostname, NE diego):
ssh root@SPIZ
cd /volume1/docker/raseliniste
sudo docker compose pull && sudo docker compose up -d
sudo docker compose logs app --tail 50
```

**Migrace** se aplikují automaticky při startu kontejneru (`prisma migrate deploy` v entrypoint). Aktuálně v DB by měly být všechny migrace do `add_upload_recording` včetně.

---

## 🗒 GLOSÁŘ POJMŮ (rozšířen 2026-05-07)

| Pojem | Co to je |
|---|---|
| **Spíž** | Modul pro sdílení souborů s 14denní expirací (`/spiz`, public `/g/<token>`) |
| **UPLOAD** | Třetí typ recordingu — host/admin nahraje hotový audio file, jen přepis bez AI analýzy |
| **canUploadAudio** | Per-host flag v ProjectInvitation pro povolení UPLOAD recordings |
| **uploadedFilename** | Pole v ProjectRecording — název souboru z disku uploadera (zobrazí se v UI místo authora) |
| **Návody** | Modul `/navody` — Petrova interní wiki ke všem modulům |
| **Decision Compass** | 5. vizualizace v Myších — custom SVG kompas se 4 kvadranty |
| **AI dotaz nad projektem** | `/api/studna/[id]/ask` — Gemini Pro nad všemi přepisy s estimate before send |

---

## 🛠 ARCHITEKTURA AUDIO RETENCE (rozšířená 2026-05-07)

| Případ | Audio se mažou? |
|---|---|
| Studánka STANDARD, host bez `keepAudio` | ✓ ano, po 14 dnech |
| Studánka STANDARD, host s `keepAudio=true` | ✗ ne, nikdy |
| Studánka STANDARD, owner | ✓ ano (pinuj přes `isPinned`) |
| Studánka BRIEF | ✗ ne, nikdy |
| Studánka **UPLOAD** (NOVÉ 05-07) | ✗ ne, nikdy (cleanup-audio cron filtruje jen STANDARD) |
| Prskavka libovolný typ | ✗ ne, nikdy (filtr `isPrivate=false`) |
| Smazání projektu | ✓ ano, okamžitě (DELETE endpoint) |
| **Spíž SharedFile** | ✓ ano, po 14 dnech (cron `cleanup-spiz` 03:10) |

**Transkripty + AI analýza** zůstávají v DB **vždy**, bez ohledu na audio file.

---

*Konec HANDOFF dokumentu. Vše klíčové je tady. Petr přechází do nové session — řekni mu „Ahoj, prošel jsem HANDOFF-2026-05-07, jsem připraven pokračovat. Na čem pracujeme?"*
